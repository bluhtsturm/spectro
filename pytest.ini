[pytest]
testpaths = tests
pythonpath = . tests
addopts = -q --strict-markers
markers =
    browser: Oberflächentest, braucht Playwright und Chromium
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
